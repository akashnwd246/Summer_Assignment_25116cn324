#include <stdio.h>
int r = 0;
void rev(int n) {
    if (n!=0) {
        r = r * 10 + (n % 10);
        rev(n / 10);
    }
}

int main() {
    int n;

    printf("Enter number: ");
    scanf("%d",&n);

    rev(n);
    printf("Reverse: %d\n",r);

return 0;
}
