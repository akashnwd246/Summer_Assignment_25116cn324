#include <stdio.h>

int sum(int n) {
    int result;

    if (n == 0) {
        result = 0;
    } else {
        result = (n % 10) + sum(n / 10);
    }

    return result;
}

int main() {
    int n;

    printf("Enter number: ");
    scanf("%d",&n);

    printf("Sum: %d\n",sum(n));

  return 0;
}
