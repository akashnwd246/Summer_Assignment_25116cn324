#include <stdio.h>
int findSum(int a, int b) {
    int result;
    result = a + b;
    return result;
}

int main() {
    int num1, num2, ans;

    printf("Enter two numbers: ");
    scanf("%d%d",&num1,&num2);

    ans = findSum(num1,num2);
    printf("Sum: %d\n",ans);
return 0;
}
